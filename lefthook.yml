pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      glob: "*.{ts,tsx,mts}"
      run: bun run typecheck
    - name: format
      glob: "*.{ts,tsx,mts,js,mjs,cjs,json,md,css,yml,yaml}"
      run: bunx prettier --check {staged_files}
