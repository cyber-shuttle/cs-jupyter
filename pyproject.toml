[project]
name = "cs-jupyter"
version = "0.1.0"
description = "Reproducible remote-only CyberShuttle JupyterLite distribution"
requires-python = ">=3.11"
dependencies = [
  "jupyterlab==4.6.3",
  "jupyterlite-core==0.8.1",
]

[tool.uv]
package = false
